---
"@treramey/graduate": patch
---

Always show a promotion row's Jira key instead of hiding keys that match the branch name, and label branches whose names carry no Jira key with a muted `no ticket` status.
