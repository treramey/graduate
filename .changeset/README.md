# Changesets

User-visible pull requests should include a changeset:

```sh
pnpm changeset
```

Select `@treramey/graduate`, choose the semver impact, and describe the change in
one sentence. Documentation-only and internal CI changes do not need one.
