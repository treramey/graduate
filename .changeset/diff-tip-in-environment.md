---
"@treramey/graduate": minor
---

Expose `tip`, `tipInEnvironment`, `unmergedAhead`, and `absorbedEnvironmentMerges` on every `gd diff` branch row (branch report schema v2) and include branches that were merged into the environment and then extended.
