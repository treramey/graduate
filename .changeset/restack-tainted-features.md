---
"@treramey/graduate": minor
---

Detect tainted features (branches that merged the environment into themselves) in `gd restack`, list them as `taintedBranches` in the schema-v3 plan, remove them by default, and never allow them to be retained.
