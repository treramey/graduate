---
"@treramey/graduate": minor
---

Add an advanced authored-date-derived commit-age report to `gd diff`, an
interactive age-report modal, consistent wide sizing across report modals, and
JSON parameters for reports scoped to several feature branches.
